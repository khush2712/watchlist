import { HtmlstripperPipe } from './htmlstripper.pipe';

describe('HtmlstripperPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlstripperPipe();
    expect(pipe).toBeTruthy();
  });
});
